import React from 'react';
import aiGif from '../assets/AI.gif'; // adjust the path if needed

const AiWidget = () => {
  return (
    <div className="ai-widget">
      <img src={aiGif} alt="AI animation"/>
    </div>
  );
};

export default AiWidget;
